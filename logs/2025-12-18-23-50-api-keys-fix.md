# Task Log - 2025-12-18 23:50

## Actions
- Investigated 404 error on `keys list` command.
- Downloaded and analyzed `openapi.json` from running server.
- Identified discrepancy in route registration (`/api/v1/api-keys` vs `/v1/api-keys`).
- Added debug logging to verify route registration code execution.
- Updated `internal/api/middleware/auth.go` to allow public access to `/api/openapi.json` and `/api/docs`.
- Rebuilt API and CLI binaries.
- Verified `keys list` now returns 401 (Unauthorized) instead of 404 (Not Found), confirming the route is correctly registered and reachable.

## Decisions
- Made OpenAPI spec endpoints public to aid debugging and follow best practices.
- Confirmed that the SDK correctly appends `/api/v1/api-keys` to the base URL.

## Next Steps
- Run the full E2E test suite to ensure all components work together.
- Verify that valid API keys work as expected (requires seeding or creating a key).

## Lessons/Insights
- Global middleware in Chi intercepts all requests, including 404s, which can mask routing issues.
- Ensuring the running binary is up-to-date with the code is crucial for debugging.
